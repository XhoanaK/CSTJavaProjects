package project9;

	import java.awt.BorderLayout;
import java.awt.GridLayout;

import javax.swing.*;

import javafx.scene.text.Text;

public class project9 extends JFrame {

	private JLabel label = new JLabel("Course Registration", JLabel.CENTER);	
	
	
	 Text nameLabel = new Text("Courses"); 
	 JButton AddButton = new JButton("ADD");
		    public static void main(String[] args) 
		    { 
		    	project9 frame = new project9();
		    	frame.pack();
		    	frame.setTitle("GUIEventDemo");
		    	frame.setLocationRelativeTo(null); // Center the frame frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); frame.setVisible(true);
		     
		    } 
		    public project9() {
		    	add(label, BorderLayout.CENTER);
//		    	JPanel jpRadioButtons = new JPanel();
//		    	 jpRadioButtons.setLayout(new GridLayout(3, 1));
//		    	      jpRadioButtons.add();
//		    	      
//		    	     add(jpRadioButtons, BorderLayout.WEST);
		    	
		    	
		    }
		}
